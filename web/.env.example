# Optional. Used only by the server-side /api/card/[owner]/[repo] embed
# endpoint to raise its shared GitHub API rate limit from 60 to 5,000
# requests/hour. Not required for the interactive scan flow, which never
# touches the server. A read-only classic PAT is enough.
GITHUB_TOKEN=your_github_pat
